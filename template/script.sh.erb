#!/usr/bin/env bash

# Benchmark info
echo "TIMING - Starting main script at: $(date)"

# Set working directory to home directory
cd "${HOME}"

# Benchmark info
echo "TIMING - Starting server at: $(date)"

# Launch the Hello World Server
set -x
python3 ${SERVER_SCRIPT} ${SERVER_CONFIG} 
